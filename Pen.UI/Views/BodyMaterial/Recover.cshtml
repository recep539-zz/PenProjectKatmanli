@model IEnumerable<Pen.DTO.BodyMaterialList>

@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<table class="table table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BodyMaterialId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BodyMaterialName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BodyMaterialId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BodyMaterialName)
                </td>
                <td>
                    <a class="btn btn-success" asp-action="Edit" asp-route-id="@item.BodyMaterialId">Edit</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.BodyMaterialId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    <a class="btn btn-dark" asp-action="List">Back to List</a>
</div>


